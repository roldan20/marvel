<h2>Comics</h2>
<hr>
<div class="row">
{{#each model }}
 <div class="col-lg-4 col-4  mb-3 ">
          <div class="card shadow p-3">
            <img style="width:100%; height: 260px;" src={{thumbnail.path}}.{{thumbnail.extension}} class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">{{title}}</h5>
              <p class="description">{{description}}</p> 
              <a href="/comic/{{id}}" class="btn btn-sm btn-dark">ver más</a>
            </div>
          </div>
        </div>
         {{/each}}
         </div>